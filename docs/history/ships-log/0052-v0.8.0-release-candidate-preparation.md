# Ship’s Log — 0052: v0.8.0 Release Candidate Preparation

**Date:** 2026-08-17
**Commander:** Sly
**Pilot:** GorXu
**Status:** RELEASE CANDIDATE PREPARED; EXACT-HEAD VERIFICATION PENDING; TAG/PUBLICATION NOT AUTHORIZED BY THIS RECORD

## Entry

Commander authorization opened issue #55 to prepare GroX `v0.8.0` from the canonical post-Program 001 baseline `main@14262546967d7aed54cf07f94759cf6e77414f24`.

The candidate aligns package/source metadata to `0.8.0` and reconciles release-facing README, Roadmap, and Progress Tracker wording with the completed Post-Apex Operational Evolution Program 001 state. No runtime capability, command path, Crew authority, Tool Gateway authority, persistence schema, verifier rule, source-provenance authority, context-runtime activation, or A6 activation behavior is widened.

## Preserved release canaries

Initial exact-head PR run `32014163449` failed in the health regression fixture because `test_source_version_detector_rejects_metadata_drift` hard-coded the prior `0.7.1` package string. After the intentional version bump, the test no longer changed its fixture and correctly failed its own mutation-effectiveness assertion. Vessel Health itself remained healthy and reported source/package metadata aligned at `0.8.0`.

The test was repaired to derive the current package version from `pyproject.toml` before injecting `9.9.9`. This preserves the required version-disagreement detector while removing coupling to a specific release number.

A subsequent Progress Tracker evidence write was caught by diff audit after replacing more of the long tracker than intended. The candidate was not allowed to proceed with that scope defect. The tracker was restored exactly from the pre-write candidate and only the bounded canary evidence was reapplied. The corrected net tracker diff returned to a small release-stewardship delta.

The self-restoration commit was authored by `github-actions[bot]`; GitHub therefore returned `action_required` rather than treating workflow-authored source as trusted qualification evidence. This checkpoint is repository-authored specifically to return the candidate to the normal protected PR CI path. The `action_required` result is preserved as trust-boundary evidence, not counted as a PASS.

## Remaining gate

Before the candidate can be considered ready for publication:

- the exact final PR head must pass Python 3.11, 3.12, 3.13, 3.14 and Wheel bootstrap portability;
- version consistency must pass in both regression runners;
- all mutation and integrated Post-Apex qualification gates must remain green;
- the final diff must receive independent exact-head verification;
- canonical `main` must remain the qualified base or the candidate must be requalified.

No `v0.8.0` tag or GitHub release has been created. Publication remains a separate Commander-controlled action. No A8 is created or implied.
